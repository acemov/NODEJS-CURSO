const {dividir} = require('./commonJS');
const {suma} = require('./commonJS');
const {resta} = require('./commonJS');
const todo = require("./commonJS")

console.log(dividir(1,2));
console.log(resta(1,2));
console.log(suma(1,2));
